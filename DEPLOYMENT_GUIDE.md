# 🚀 คู่มือการ Deploy Axie Clone RPG

## 📋 สิ่งที่ต้องเตรียม

1. บัญชี GitHub
2. บัญชี Supabase (ฟรี)
3. บัญชี Vercel/Netlify (ฟรี) สำหรับ hosting

---

## ขั้นตอนที่ 1: ตั้งค่า Supabase Database

### 1.1 สร้าง Project ใน Supabase

1. ไปที่ [https://supabase.com](https://supabase.com)
2. คลิก "Start your project"
3. สร้าง Organization ใหม่ (ถ้ายังไม่มี)
4. คลิก "New Project"
5. ตั้งชื่อ project: `axie-clone-rpg`
6. ตั้ง Database Password (เก็บไว้ให้ดี!)
7. เลือก Region: `Southeast Asia (Singapore)` (ใกล้ไทยที่สุด)
8. คลิก "Create new project"
9. รอ 2-3 นาทีให้ project สร้างเสร็จ

### 1.2 สร้าง Database Tables

1. ไปที่ SQL Editor (เมนูด้านซ้าย)
2. คลิก "New query"
3. คัดลอกโค้ดจากไฟล์ `supabase-schema.sql` ทั้งหมด
4. วางลงใน SQL Editor
5. คลิก "Run" (หรือกด Ctrl+Enter)
6. ตรวจสอบว่าไม่มี error

### 1.3 เอา API Keys

1. ไปที่ Settings > API (เมนูด้านซ้าย)
2. คัดลอก:
   - `Project URL` (จะเป็น https://xxxxx.supabase.co)
   - `anon public` key (ใน Project API keys)
3. เก็บไว้ใช้ในขั้นตอนถัดไป

---

## ขั้นตอนที่ 2: ตั้งค่า GitHub Repository

### 2.1 สร้าง Repository

1. ไปที่ [https://github.com/new](https://github.com/new)
2. ตั้งชื่อ repository: `axie-clone-rpg`
3. เลือก Public หรือ Private (แนะนำ Public)
4. **ไม่ต้อง** เลือก "Add README" หรือ ".gitignore"
5. คลิก "Create repository"

### 2.2 Push โค้ดขึ้น GitHub

เปิด Terminal/Command Prompt ในโฟลเดอร์โปรเจค แล้วรันคำสั่ง:

\`\`\`bash
# Initialize git (ถ้ายังไม่ได้ทำ)
git init

# Add all files
git add .

# Commit
git commit -m "Initial commit: Axie Clone RPG with Supabase"

# เชื่อมกับ GitHub (เปลี่ยน YOUR_USERNAME เป็นชื่อ GitHub ของคุณ)
git remote add origin https://github.com/YOUR_USERNAME/axie-clone-rpg.git

# Push ขึ้น GitHub
git branch -M main
git push -u origin main
\`\`\`

---

## ขั้นตอนที่ 3: Deploy ด้วย Vercel (แนะนำ)

### 3.1 เชื่อมต่อ Vercel กับ GitHub

1. ไปที่ [https://vercel.com](https://vercel.com)
2. คลิก "Sign Up" และเลือก "Continue with GitHub"
3. อนุญาตให้ Vercel เข้าถึง GitHub

### 3.2 Import Project

1. คลิก "Add New..." > "Project"
2. เลือก repository `axie-clone-rpg`
3. คลิก "Import"

### 3.3 ตั้งค่า Environment Variables

ใน Configure Project:

1. เปิด "Environment Variables"
2. เพิ่ม 2 ตัวแปร:

\`\`\`
Name: VITE_SUPABASE_URL
Value: https://xxxxx.supabase.co (จาก Supabase)

Name: VITE_SUPABASE_ANON_KEY
Value: eyJxxx... (anon key จาก Supabase)
\`\`\`

3. คลิก "Deploy"
4. รอ 2-3 นาที
5. เสร็จแล้ว! 🎉

---

## ขั้นตอนที่ 4: ทดสอบเว็บไซต์

1. Vercel จะให้ URL เช่น `https://axie-clone-rpg.vercel.app`
2. เปิด URL นั้นในเบราว์เซอร์
3. ทดสอบ:
   - สร้างตัวละครใหม่ใน Admin Panel
   - เล่นเกมในโหมด Battle
   - ตรวจสอบว่าข้อมูลถูกบันทึกใน Supabase

---

## ขั้นตอนที่ 5: ตั้งค่า Custom Domain (Optional)

### ใช้ Domain ของ Vercel (ฟรี)

1. ไปที่ Project Settings > Domains
2. เพิ่ม domain ที่ต้องการ เช่น `axie-rpg.vercel.app`

### ใช้ Domain ของตัวเอง

1. ซื้อ domain จาก Namecheap, GoDaddy, หรือ Cloudflare
2. ไปที่ Project Settings > Domains
3. เพิ่ม domain ของคุณ
4. ตั้งค่า DNS ตามที่ Vercel บอก

---

## 🔧 การอัปเดตเว็บไซต์

เมื่อคุณแก้ไขโค้ด:

\`\`\`bash
# Add changes
git add .

# Commit
git commit -m "Update: คำอธิบายการเปลี่ยนแปลง"

# Push to GitHub
git push

# Vercel จะ auto-deploy ให้อัตโนมัติ!
\`\`\`

---

## 📊 ตรวจสอบข้อมูลใน Supabase

1. ไปที่ Supabase Dashboard
2. คลิก Table Editor (เมนูด้านซ้าย)
3. เลือก table ที่ต้องการดู:
   - `characters` - ตัวละครทั้งหมด
   - `cards` - การ์ดทั้งหมด
   - `character_cards` - ความสัมพันธ์ระหว่างตัวละครและการ์ด

---

## 🐛 แก้ปัญหาที่พบบ่อย

### ปัญหา: เว็บไซต์ขึ้น "Failed to fetch"

**วิธีแก้:**
1. ตรวจสอบว่า Environment Variables ใน Vercel ถูกต้อง
2. ตรวจสอบว่า Supabase project ยังทำงานอยู่
3. ตรวจสอบ RLS Policies ใน Supabase

### ปัญหา: ไม่สามารถบันทึกข้อมูลได้

**วิธีแก้:**
1. ตรวจสอบ RLS Policies ใน Supabase
2. ตรวจสอบ Console ใน Browser (F12) เพื่อดู error
3. ตรวจสอบว่า API Keys ถูกต้อง

### ปัญหา: รูปภาพไม่แสดง

**วิธีแก้:**
1. ตรวจสอบว่ารูปถูกบันทึกเป็น Base64 ใน database
2. ตรวจสอบขนาดรูป (ไม่ควรเกิน 1MB)
3. ลองใช้ Supabase Storage แทน Base64

---

## 🎯 ขั้นตอนถัดไป (Optional)

1. **เพิ่ม Authentication:**
   - ใช้ Supabase Auth
   - ให้ผู้เล่นแต่ละคนมีตัวละครของตัวเอง

2. **เพิ่ม Multiplayer:**
   - ใช้ Supabase Realtime
   - ให้ผู้เล่นต่อสู้กันได้

3. **เพิ่ม Leaderboard:**
   - บันทึกคะแนนการชนะ
   - แสดง Top 10 players

4. **ใช้ Supabase Storage:**
   - เก็บรูปภาพใน Storage แทน Base64
   - ลดขนาด database

---

## 📞 ต้องการความช่วยเหลือ?

- Supabase Docs: https://supabase.com/docs
- Vercel Docs: https://vercel.com/docs
- Vite Docs: https://vitejs.dev/guide/

---

**สร้างโดย:** Axie Clone RPG Team  
**เวอร์ชัน:** 1.0.0  
**อัปเดตล่าสุด:** 2024
